#include "main.h"
/**
 * add - prints function that adds two integers and returns the result
 * @a: first number
 * @b: second number
 * Return: the addition of both
 */
int add(int a, int b)
{
	return (a + b);
}
